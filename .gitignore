# Locally-copied dev manifest (see "Local dev testing" in PRD.md / PROMPTS.md GD-1).
# The real manifest.json is generated by GD-2's Actions workflow and committed by CI, never
# hand-copied here — any manifest.json present during local development is a throwaway copy
# of manifest.sample.json and must never be committed.
manifest.json
